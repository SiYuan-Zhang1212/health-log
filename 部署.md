# 部署到 Cloudflare（一次性，约 20 分钟）

目标：把「健康日志」放到自己的域名上，任何设备任何网络都能打开，数据存在你 Cloudflare 账号下的 D1 数据库里。
费用：域名已经买好了（`siyzhang12.online`），后续 $0。

> **你的域名 `siyzhang12.online` 已托管在 Cloudflare**（NS = `bart`/`kelly.ns.cloudflare.com`，已验证）。
> 所以「添加站点、改 NS」这步可以完全跳过；`wrangler.jsonc` 里已经配好部署时自动创建
> `health.siyzhang12.online`。

---

## 0. 前置条件

- 域名 `siyzhang12.online` ✅ 已完成
- Cloudflare 账号 ✅ 已完成
- Node.js 18+（你已装 v24）
- `wrangler` 已经装好（`node_modules/` 里，v4.130.0），直接 `npx wrangler` 即可
- 配置预检已通过：24 个静态文件、总上传 17 KiB（gzip 5 KiB），D1 与 Assets 绑定正常

## 1. 安装 wrangler 并登录

```bash
cd /Users/zhangsiyuan/Documents/5th1st/体重管理自制
npm install
npx wrangler login          # 会打开浏览器授权，选你的 Cloudflare 账号
```

## 2. 创建 D1 数据库

```bash
npx wrangler d1 create health-log
```

输出里有一行 `database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"`，
把它填进 `wrangler.jsonc` 的 `d1_databases[0].database_id`（现在是全 0 的占位符）。

## 3. 建表

```bash
npm run db:schema           # 等于 wrangler d1 execute health-log --file=worker/schema.sql --remote
```

## 4. 设置密码和密钥

```bash
npx wrangler secret put APP_PASSWORD      # 登录密码，自己定，记牢
npx wrangler secret put SESSION_SECRET    # 会话签名密钥，建议：openssl rand -base64 32
npx wrangler secret put CLI_TOKEN         # 命令行工具用的令牌，建议：openssl rand -hex 24
npx wrangler secret put DEEPSEEK_API_KEY  # 可选；不设就用网页「设置」里保存的那份
```

四个都存成 Cloudflare 加密 Secret，不会出现在代码或 Git 里。

## 5. 先本地跑一遍（可选但推荐）

```bash
npx wrangler dev            # 打开 http://localhost:8787
```

首次会用本地 D1，需要再建一次表：

```bash
npm run db:schema:local
```

用你设的 APP_PASSWORD 登录，确认能进、能记录、能保存。

## 6. 部署

```bash
npm run deploy
```

`wrangler.jsonc` 里已经声明了自定义域名，所以部署时会**自动创建 `health.siyzhang12.online`**
并签发证书（首次可能要等 1-3 分钟生效）。同时也会给你一个 `https://health-log.<账号子域>.workers.dev`
作为备用地址。

## 7. 域名

不用做任何事——上一步已经自动绑好。验证：

```bash
dig +short health.siyzhang12.online     # 有 A/AAAA 记录即生效
```

想换成别的子域名（比如 `log.siyzhang12.online`），改 `wrangler.jsonc` 里的 `routes` 再 `npm run deploy`。

## 8. 把现有数据搬上去

本机数据在 `data/health.json`（已在 Git 里忽略，不会外泄）。用命令行推上去：

```bash
python3 cli.py remote https://health.siyzhang12.online <第4步的 CLI_TOKEN>
python3 cli.py push          # 强制覆盖云端（本地数据为准）
python3 cli.py today         # 验证：读的是云端数据
```

之后 `cli.py` 的所有命令都直接读写云端，AI 也走云端代理（Key 不用再放本地）。

## 9. 上线检查清单

- [ ] 打开 `https://health.siyzhang12.online` → 出现登录页（不是直接进首页）
- [ ] 密码输错 → 提示「密码不对」，连错 10 次会被限流 15 分钟
- [ ] 登录后首页数据完整（三餐/训练/体测/目标）
- [ ] 手机浏览器打开 → 「添加到主屏幕」，像 App 一样
- [ ] 手机记一笔 → 电脑刷新能看到（同一份数据）
- [ ] 设置页 → DeepSeek Key 显示「已配置」，AI 能出结果
- [ ] 设置页 → 云端快照 / 恢复 能列出快照
- [ ] `python3 cli.py today` 读到云端数据
- [ ] 退出登录后回到登录页

## 10. 日常维护

| 事项 | 怎么做 |
|---|---|
| 改密码 | `npx wrangler secret put APP_PASSWORD`（旧会话 30 天内仍有效，想立刻踢掉就同时改 `SESSION_SECRET`） |
| 看用量 | Cloudflare 控制台 → Workers → 指标；D1 → Metrics |
| 恢复数据 | 网页「设置 → 云端快照 / 恢复」，或 `wrangler d1 time-travel restore` |
| 换域名 | 控制台改 Custom domain，`cli.py remote` 重新配一下 |
| 回退到本地 | 不配 `remote` 就是本地模式，`python3 server.py` 照常用 |
| 重新部署 | 改完代码 `npm run deploy` |

## 11. 想再加一层防护（可选）

网页登录已经够用。如果还想连页面本身都不给外人看：

Cloudflare 控制台 → Zero Trust → Access → Applications → Add self-hosted → 域名填 `health.siyzhang12.online`，策略设为「邮箱验证码」。免费版支持最多 50 个用户。
这样未通过 Access 的人连 HTML 都拿不到，进去之后还有应用自己的密码。

## 12. 免费额度对照（你的实际用量）

| 资源 | 免费额度 | 你的用量 |
|---|---|---|
| Workers 请求 | 10 万/天 | 重度使用约 200/天 |
| D1 存储 | 5 GB（单库 500 MB） | 一年约 0.6 MB |
| D1 行写入 | 10 万/天 | 约 100/天 |
| D1 行读取 | 500 万/天 | 约 200/天 |
| 静态资源 | 无限请求、无限带宽 | 每次打开约 180 KB |
| 出口流量 | 免费 | — |

结论：额度用不完，成本就是一个域名。

## 13. 排错

| 现象 | 原因 / 处理 |
|---|---|
| 登录后立刻又跳回登录页 | Cookie 没存住：确认访问的是 `https://` 域名，不是 IP |
| `服务器未设置 APP_PASSWORD` | 第 4 步的 secret 没设，或设完没重新 deploy |
| 保存时报「冲突」 | 另一台设备刚写过，前端会自动刷新重试；持续出现就刷新页面 |
| 401 且 CLI 报鉴权失败 | `cli.py remote` 里的 token 和 `CLI_TOKEN` 不一致 |
| AI 报「还没有配置 DeepSeek API Key」 | 网页「设置」填 Key，或设 `DEEPSEEK_API_KEY` secret |
| 页面能开但数据是空的 | 还没 push：第 8 步 |
| 本地开发时登录页不出现 | 本地 `server.py` 不做鉴权，直接进；这是预期行为 |

## 14. 改代码后怎么验证

```bash
npm test                    # 41 项 API 行为测试（内存 D1，不需要联网）
python3 server.py --port 8099 --no-open   # 本地模式回归
```
