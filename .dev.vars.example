# 本地开发用的环境变量（`wrangler dev` 会读取）
# 复制成 .dev.vars 后填上自己的值；.dev.vars 已在 .gitignore 里，不会被提交
APP_PASSWORD=dev-password
SESSION_SECRET=dev-only-not-for-production
CLI_TOKEN=dev-cli-token
# DEEPSEEK_API_KEY=sk-xxx
